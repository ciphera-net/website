// Auto-generated from WordPress at build time — do not edit manually.
// Run: npm run generate:blog
//
// 🔴 BUILD OUTPUT, NOT SOURCE. Every build overwrites this.
// The committed copy is an EMPTY STUB and must stay empty: it exists only so the
// module resolves on a fresh clone, before the first `npm run generate:blog`. If a
// real post ever appears here in a diff, the commit is wrong — the bodies belong in
// WordPress, and a committed copy is a second source of truth for what the CMS says.
//
// The HTML this holds is WordPress's rendered output with FAQ blocks lifted out. It is
// SANITISED AT RENDER TIME by components/blog/wp-body.tsx, not here — one allowlist,
// applied where the markup becomes elements.

import type { WpBlogPost } from './blog-types'

export const wpPosts: WpBlogPost[] = []
